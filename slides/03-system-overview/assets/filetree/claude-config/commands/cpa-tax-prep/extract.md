# Extract

Spawns parallel tax-extract subagents — one per source
document. Each subagent gets intake context and type-
specific instructions. Outputs are aggregated into
raw/ category files after all subagents complete.
